First iteration of the readme. For now onmy the tool that allow to call the exchage rate api is implemented. More precisely, it call the endpoint pair with two currencies and an amount as parameters and return the right amount in the targeted currencie.


to try it, just execute test.y
